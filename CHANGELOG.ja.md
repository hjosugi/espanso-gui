# 変更履歴

[English](CHANGELOG.md) | [日本語](CHANGELOG.ja.md)

Espanso GUIの重要な変更を記録します。形式は[Keep a Changelog](https://keepachangelog.com/ja/1.1.0/)に従い、バージョンにはセマンティックバージョニングを使用します。

## [未リリース]

## [0.3.1] - 2026-08-20

### 追加

- 対応する全content／variable種類、default／app-specific profile、diagnostic、将来互換の未知YAML値を含む、破棄可能なnative accessibility監査用標準fixtureを追加。
- production storage codeで監査matrix全体を読み込み、意図した編集後も未知dataを保持することを検証する回帰testを追加。
- advisory、yanked／unsound crate、license、wildcard dependency、package sourceを検査する`cargo-deny` policyとCI jobを追加。

### 変更

- 再現可能なaccessibility監査の準備、merge-conflictの発生方法、fixture reset、実行を意図しないshell／script例の安全な扱いを日英の手順書へ記載。
- Rust 1.95互換のtransitive dependencyを更新し、compatibility文書とbug report templateに残っていたversion固定表現を修正。

## [0.3.0] - 2026-08-16

### 変更

- 主要ライブラリ、スニペット／プロファイル編集、診断、設定、履歴、初期設定、操作結果、変数ビルダー、フォームビルダー、モーダルダイアログの日本語／英語対応を拡張。
- 言語に依存しないモデルへ日本語初期値を埋め込まず、新規スニペットの初期テンプレートを選択言語に合わせて表示。
- 新規変数の例、名称未設定時の表示、日付書式プリセット、競合概要、ストレージ／検証エラーをコアモデルの日本語直書きから選択言語による表示へ変更。
- Espansoコマンドの空の結果と外部リンクを開けない場合の表示をローカライズし、文書リンクの起動失敗を破棄せず通知。
- エディター、プロファイル、変数、フォーム、Raw YAML、言語、拡大率、画像プレビュー、スニペットカード、ダイアログ、ライブ状態のセマンティクスをAccessKitツリーへ接続。プラットフォームに合ったショートカット表示とWCAG AAパレット／フォーカス検査を拡張。
- カスタムスニペットカードが支援技術によるフォーカスを拒否する問題を修正。繰り返し表示されるプロファイル設定と行操作に、対象が分かる一意な読み上げ名を付与し、LinuxのOrcaで検証。
- UI拡大率の両方の操作部に直接アクセシブル名を付与。モーダルのダイアログノードが項目と操作を構造的に所有するよう変更。日英両言語の画面全体AccessKit回帰テストを追加し、フォーカス名／順序と全ダイアログを検証。8種類すべてのダイアログについて、背後のグローバルナビゲーションや編集ショートカットが動作しないことを入力テストで確認。
- 高いUI／OS拡大率と狭い論理ビューポート向けに、コンパクトナビゲーション、狭いリストパネル、縦積みフィールド、ビューポート内でスクロールするダイアログを追加。競合エディターは200%表示時に760×560ポイントの固定最小サイズを要求しないよう変更し、最大拡大率のツリーテストで両言語の主要画面と変数／フォーム／競合操作を確認。
- コンパクト表示だけ空状態の上余白を抑え、200%でも初期設定内容が早く初期表示へ入るよう調整。長い設定パスは共通部品で選択可能かつ折り返し可能にし、横方向の押し出しを防止。
- 色とtint opacity、文字体系、間隔／余白、操作部／境界線の形状、パネル／リスト／フィールド／モーダルの寸法、塗りつぶし／コンテキスト操作、選択リストの配置、モーダル操作の配置、長文ページ幅を再利用可能なビジュアルシステムへ集約。文字サイズを32／24／20／18ポイントだけに制限し、ページタイトルとセクション見出しに明確な階層を設定。ナビゲーション行を一貫した利用可能幅に統一し、UIコード内のデザイントークン外の数値をソーステストで拒否。
- 画面別の個別調整ではなく、全体の文字、操作部／list rowの高さ、panel padding、field幅、navigation／list幅の基準値を拡大。1行／複数行の全editorは左右16／上下12ポイントの読みやすい内側余白へ統一。
- 残っていたcompactなツールキット既定値を、追加行間4ポイント、check／radio icon 24ポイント、icon間隔12ポイント、広いslider／selector、modal余白24ポイント、menu余白12ポイントへ変更。
- hoverまで隠れる2ポイントのfloating scroll handleを、常時見える12ポイントのsolid scroll barと最小48ポイントのhandleへ変更。
- placeholder／補助文字のcontrastを高め、page背景だけでなく実際の入力欄／無効操作面に対するWCAG AAも検証。
- すべての1行入力欄を左右16／上下12ポイント余白の共通部品へ統一。主要画面とダイアログのアクセシビリティツリーを使い、フォーカス可能な操作部が高さ48ポイント未満にならないことを回帰テストで検証。
- 残っていたcompactなeditor／row action buttonを共通の20ポイント、40×48ポイント操作部へ変更。status badgeも共通の16×12ポイントinsetを使用。
- 既定viewport／200% zoomで名前のあるUI nodeのboundsを検証する回帰testを追加。はみ出していたform action、variable action、profile mode selector、expansion type controlを共通responsive detail／action layoutで修正。
- 200%拡大時も詳細editorの実用幅を残すようsnippet／profile一覧幅を自動調整し、key、quoted value、commentを見分けられるcache付き・高contrastなRaw YAML色分けを追加。
- 日英の検索placeholderを短くし、200%拡大時のcompact入力欄に省略なしで収まることを実フォント幅で検証。
- 複数行になるcompact navigationを画面selector 1つへ置換し、再読み込みを折り返すcompact top barからworkspace行へ移動。200%拡大時も選択画面の値と全画面の最初の主要surfaceが見えて支援技術へ公開されることを検証。
- コンパクト上部バーの色だけに依存する接続状態の点を、日英の文字付き状態表示へ変更。200%の最小幅でも接続／未検出の両文言が収まることを実フォント幅で検証。
- 画面幅に応じたワークスペースナビゲーションを、型付き操作を返す独立した表示モジュールへ分離。ファイルが増えても一覧だけがスクロールし、最小ウィンドウ高で「ファイルを追加」、version、「設定」、「このアプリについて」が重ならず画面内に残ることを回帰テストで確認。
- 画面幅に応じた上部バーを副作用のない表示モジュールへ分離し、型付き保存／再読込／Espanso再起動操作をアプリ状態層へ返す構造に変更。
- 引用値内の`#`を行内コメントと誤認せず、変更したブロックスカラー項目の古い継続行も残さず、CRLFと末尾改行なしの形式も維持するよう、YAML保持処理を修正。引用符を認識する走査処理をRaw YAML色分けとコメント警告の判定で共有。
- 再帰的な複製を防ぐため、使用中のEspanso設定フォルダ内を一括バックアップ先に選ぶ操作を拒否。同一時刻の保存先が重なった場合も、以前の書き出しを上書きせず別ディレクトリを選択。
- 設定画面の表示とファイル／プロセス操作を分離。操作対象がない、または読み取り専用の場合は、失敗するだけのバックアップ、CSV入出力、ファイル削除を受け付けず無効状態で表示。
- 通常／正規表現トリガーで選択した方式が直ちに保持されない不具合を修正。既存の通常トリガーをエスケープして正規表現へ引き継ぎ、切替時の文字列消失を防止。すべての方式／タブに常時見えるボタン境界を付け、選択中の方式には色以外のチェック印も表示し、読み上げ名は安定させる。
- 保存されるシステム／ライト／ダーク外観とコントラスト検証済みの配色を追加。選択行／タブを高コントラストの塗り、太字ラベル、色に依存しないチェック印、選択カード端の印で強調し、読み上げ名は安定させる。ナビゲーションのラベルとショートカットを1行で整列し、作業フォルダ未接続時はファイル作成操作を非表示化。
- 無効な操作部は判別できる見た目を保ちながらツールキット既定値より不透明にし、実際の無効背景へ合成した文字色がWCAG AAコントラストを満たすことを検証。
- ページタイトルとセクションタイトルを32ポイント／24ポイントの視覚階層を保ったまま、レベル1／レベル2のアクセシビリティ見出しとして公開。
- 診断を言語非依存のモデルイベントへ変更し、表示を型付き翻訳カタログへ移動。
- ストレージ検証失敗を型付きIssueへ変更し、選択言語で表示。
- HTML断片生成と実行しないテキストプレビューをアプリUI状態から分離し、安全性の単体テストを追加。
- 未知のフォーム項目タイプを、ユーザーが対応タイプへ明示的に変更するまでビジュアルエディター内で保持。
- Narrator、VoiceOver、Orcaによるネイティブ監査表を追加し、キーボード、スクリーンリーダー、拡大率、ローカライズ、データ保持を確認。
- フローティングエディターダイアログを入力遮断モーダルに変更し、ダイアログ背後のグローバルショートカットと操作を無効化。
- リリースのパッケージjobを読み取り専用GitHub tokenへ制限し、タグで制限された公開jobだけにrelease書き込み権限を付与。署名済みmacOS appをパッケージ後検証用に保持し、artifact identity、Team ID、安全なtimestamp、hardened runtime、Gatekeeper評価、disk image integrity、notarization ticketを検証。
- Windowsのリリース検証を強化し、すべてのAuthenticode署名について、有効性、設定証明書thumbprintとの一致、RFC 3161 timestamp証明書、全署名検証を必須化。
- checkout認証情報の保持を無効化。リリース権限、未署名パッケージ、認証情報の完全性gate、Windows binary／installer署名、macOS app保持／配布検証、署名状態note、checksum添付を検証するRust回帰テストを追加。
- README、利用者／開発者向け文書、Issue仕様、リリースノート、GitHub入力テンプレート、AppStreamメタデータを日本語／英語対応にし、文書ペアの欠落を検出する回帰テストを追加。
- 入力中のスニペット検索を読み込み済みの全match fileへ拡張し、結果のsource path、日英の件数表示、検索結果なし状態とclear操作を追加。件数更新を名前付きpolite accessibility announcementとして公開。
- Espanso file自体を書き換えずに、スニペット一覧をYAML順／名前順／トリガー順で並べ替えて選択を保存する機能を追加。既存の保存設定は既定でYAML順を維持。
- Espanso標準の`search_terms`を、全ファイル対象のタグ絞り込みとして表示。日英の明示ラベルと使用件数を備え、GUI専用fieldをユーザーYAMLへ追加しない設計を維持。
- 未接続workspaceをresponsiveな日英onboarding画面へ変更。Espanso導入案内、公式文書への操作、接続対象の正確なpath、初期化で既存fileを上書きしない説明を追加し、Aboutと外部link失敗処理を共通化。
- 全ファイル検索、標準tag集約、並べ替え、list entry生成をapplication renderingから副作用のない`snippet_library` moduleへ分離。
- 永続設定、設定画面、トリガー方式／ツールバー、プロファイル操作部、変数タイプ別操作部、応答型のラベル付き入力欄を、それぞれ目的別モジュールへ分離し、アプリケーション状態モジュールの肥大化を抑制。
- 対応済みworkflow、意図的なproduct境界、残る差分、release UX gateを記録する日英のaText／Dash UX benchmarkを追加。

## [0.2.0] - 2026-08-15

### 追加

- フィルター、バックエンド、遅延、ショートカット、フォームサイズを扱うアプリ別設定画面。
- フィールド単位でlocal／diskを選べるローカルthree-way YAML mergeと復元可能な履歴。
- 決定的なマークアップを生成し、実行を伴わないプレビューを備える安全なHTMLビジュアルエディター。
- 日本語／英語UI、キーボード操作、アクセシブルな検索ラベル、80～200%の拡大率。
- リリースごとに署名状態を表示する、任意のWindows Authenticode署名とmacOS Developer ID署名／notarization。

### 変更

- 構造化されたmatch、global variable、profileの編集で、コメント、アンカー、ブロックスカラー、引用形式、未知フィールドを含む、変更されていないYAMLの全バイトを保持。
- `sha2`を0.11へ更新し、小文字digest encodingを明示。

## [0.1.0] - 2026-08-15

### 追加

- Windows、macOS、Linux向けネイティブRust GUI。
- テキスト、Markdown、HTML、画像、フォームのビジュアルエディター。
- ローカル／グローバル変数のガイド付きビルダー。
- YAML診断、Raw YAML編集、未知フィールドを保持する将来互換性。
- 楽観的同時実行制御、atomic write、自動バックアップ、復元可能な削除。
- CSV入出力、手動スナップショット、Espansoサービス操作。
- クロスプラットフォームCIとリリースパッケージ作成。

[未リリース]: https://github.com/hjosugi/espanso-gui/compare/v0.3.1...HEAD
[0.3.1]: https://github.com/hjosugi/espanso-gui/compare/v0.3.0...v0.3.1
[0.3.0]: https://github.com/hjosugi/espanso-gui/compare/v0.2.0...v0.3.0
[0.2.0]: https://github.com/hjosugi/espanso-gui/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/hjosugi/espanso-gui/releases/tag/v0.1.0
